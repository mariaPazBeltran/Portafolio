import InitialState from './InitialState'
function reducer(state, action) {
    switch (action.type) {
    case 'new':
        
        return 

    case 'reset':
        return InitialState
    default:
        throw new Error()
    }
}
export default reducer
